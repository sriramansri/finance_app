import pool from "../dbConfig.mjs";
import jwt from "jsonwebtoken";

export const loanForm = async (req, res) => {
    try{
        if (!req.user) {
            
        }
    }
    catch{
        
    }
}; 
